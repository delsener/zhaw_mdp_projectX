package ch.zhaw.mdp.projectX.strategy;

public interface CalculationStrategy {

	/**
	 * Calculate with the given two int's.
	 * 
	 * @param a number one.
	 * @param b number two.
	 * @return the result of the calculation.
	 */
	int calculate(int a, int b);
	
}
